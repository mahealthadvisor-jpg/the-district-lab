rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Phase 1A baseline: authenticated users only. Tighten in Phase 1B/1C.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
